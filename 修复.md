# 审查结果修复执行文档

本文档用于收口提交 `61fb86bc`（`refactor: simplify generated clients and runtime`）的审查问题。执行顺序为：先修复生成物漂移门禁，再在 CI 中强制该门禁并完成跨平台验证，最后处理“五个独立 PR”与当前单提交之间的交付偏差。不改变 HTTP 路径、OpenAPI 数据结构、UI 行为、缓存语义或 Rust 运行时功能。

## 当前基线

- 当前分支：`main`，比 `origin/main` 超前 1 个提交。
- 当前实施提交：`61fb86bc`。
- 已知用户文件：`.codegraph/` 和 `docs/zh-CN/project-functional-analysis.md`，全程不得删除、覆盖、暂存或提交。
- 已通过的基线：UI 153 项测试、`cargo check --workspace --all-targets`、Rust fmt、workspace Clippy，以及文件锁、掩膜裁剪、字体缓存、并行度、OpenAPI 默认路径五项等价性测试。

## 范围

- 包含：生成物新增未跟踪文件检测、CI 生成物门禁、macOS/Windows/Linux 构建与测试验证、交付结构收口、最终审查。
- 不包含：功能开发、API 调整、ML/渲染算法修改、Orval 替换、Faker 传递依赖强制删除、既有 UI warning 清理、CUDA macOS 基线测试修复、无关文档修改。

## 成功标准

- `bun run check:generated` 能同时发现已跟踪文件变化和生成目录中的新增未跟踪文件。
- GitHub Actions 在 PR 上自动运行生成物漂移检查。
- macOS、Windows 2022、Ubuntu x64 和 Ubuntu ARM 构建矩阵全部通过；Linux workspace tests 和 UI tests 通过。
- 不新增直接依赖，不恢复已删除的生成客户端、MSW 随机 handler 或 React Query 生成层。
- HTTP 路径数仍为 36，手写 query key 、错误类型和缓存配置不变。
- 最终 `git status --short` 只允许出现两个已知用户未跟踪路径。

## 执行步骤

### [ ] 1. 建立安全检查点

1. 记录当前状态：

   ```bash
   git status --short
   git log -1 --oneline
   git branch -vv
   ```

2. 确认 `HEAD` 为 `61fb86bc`，并且只有 `.codegraph/` 和 `docs/zh-CN/project-functional-analysis.md` 两个已知未跟踪路径。如果出现其他修改，停止执行并先确认归属。
3. 在任何历史拆分前创建保护分支：

   ```bash
   git branch backup/simplification-61fb86bc 61fb86bc
   ```

通过标准：保护分支指向 `61fb86bc`，工作区内用户文件未变。

### [ ] 2. 实现跨平台生成物漂移检查器

1. 新增 `scripts/check-generated.ts`，只执行验证，不调用生成器。
2. 检查器必须使用参数数组调用 Git，不依赖 Bash 字符串拼接，以便 Windows 和 Unix 一致运行。
3. 依次检查以下路径：
   - `ui/openapi.json`
   - `ui/lib/api/generated.ts`
   - `ui/lib/api/generated.msw.ts`
   - `ui/lib/api/schemas`
4. 通过 `git status --porcelain=v1 --untracked-files=all -- <paths>` 一次检测已暂存、未暂存、已删除和新增未跟踪生成物。
5. 任一检查失败时，输出具体文件列表并以非零状态退出；全部干净时以 0 退出。
6. 修改根 `package.json`：
   - 新增 `verify:generated` 脚本，运行 `scripts/check-generated.ts`。
   - 将 `check:generated` 改为先运行 `generate:api`，再运行 `verify:generated`。

通过标准：正常生成后检查成功，且检查器不依赖 Unix 专有命令。

### [ ] 3. 为未跟踪生成物添加回归测试

1. 将“生成”和“验证”保持为两个可独立执行的脚本，避免 Orval `clean` 在测试前删除探针文件。
2. 先运行：

   ```bash
   bun run generate:api
   bun run verify:generated
   ```

   预期两条命令都成功。
3. 在 `ui/lib/api/schemas/` 下临时创建一个未跟踪探针文件，单独运行 `bun run verify:generated`，预期命令失败且输出探针路径。
4. 立即删除探针文件，再次运行 `bun run verify:generated`，预期恢复成功。
5. 用 `try/finally` 或等价清理机制自动删除探针，避免测试中断污染工作区。

通过标准：无漂移时返回 0，已跟踪文件变化或新增未跟踪文件时都返回非 0，测试结束后不留探针文件。

### [ ] 4. 把生成物检查接入 CI

1. 修改 `.github/workflows/test.yml`。
2. 在已安装 Linux/Tauri 系统依赖的 Rust job 中加入 Bun setup 和 Bun 缓存，然后执行 `bun install --frozen-lockfile`。
3. 在 workspace tests 之后增加独立的 `Test generated artifact checker` 和 `Check generated artifacts` 步骤，后者执行：

   ```bash
   bun run check:generated
   ```

4. 不把检查放入只安装 Node/Bun 的 UI job，因为 `generate:openapi` 需要 Rust workspace。

通过标准：PR 上修改 OpenAPI 或 Orval 输入后，未提交对应生成物会使 Test workflow 失败。

### [ ] 5. 运行本地验证

按以下顺序执行，任一命令失败都先修复，不带失败进入下一阶段：

```bash
bun run format:check
bun run lint:ui
bun run test:ui
bun cargo fmt -- --check
bun cargo check --workspace --all-targets
bun cargo clippy --workspace -- -D warnings
bun cargo test -p koharu-app exclusive_lock_prevents_second_open
bun cargo test -p koharu-app inpainting_engines_share_identical_region_clipping
bun cargo test -p koharu-renderer fontdue_cache_initializes_once_across_font_clones
bun cargo test -p koharu-runtime host_parallelism_is_never_zero
bun cargo test -p koharu-rpc --bin openapi app_data_default_is_platform_neutral
bun run check:generated
bun run build
git diff --check
git status --short
```

补充检查：

- 确认 `ui/openapi.json` 仍有 36 条 path，无新增或删除。
- 确认 `ui/lib/api/index.ts` 的 9 个 query key 值与改造前一致。
- 确认 `@mswjs/data`、`xstate`、`@radix-ui/react-switch` 不在直接依赖或锁文件中。
- 确认 `@faker-js/faker` 仅由 Orval 可选 peer 传递引入，不在 `ui/package.json` 中直接声明。

已知非阻断项：

- `bun run lint:ui` 可以保留审查前已存在的 14 条 warning，但不允许新增 warning。
- macOS ARM 上的 `cuda::tests::source_id_includes_platform` 为已知基线失败，本修复不修改 CUDA 逻辑；Linux CI 仍必须完整运行 `cargo test --workspace --tests`。
- `cargo clippy --workspace --all-targets -- -D warnings` 的 `identity_op` 基线不在本次范围；标准 workspace Clippy 必须通过。

### [ ] 6. 收口单提交与五 PR 计划的偏差

默认采用“保留已验证的聚合提交，不为形式目标冒险重写大型已验证差异”的方案：

1. 保留 `61fb86bc`，不在当前 `main` 上执行 `reset --hard`、不重建 462 个文件的分批锁文件状态。
2. 将生成物门禁修复作为第二个聚焦提交，推荐提交信息：

   ```text
   fix: harden generated artifact drift checks
   ```

3. 在 PR 说明中明确列出原 PR1～PR5 的五个逻辑批次、各自验证证据和“本地实施最终聚合为一个提交”的偏差。
4. 将“五个独立 PR”标记为经过审查接受的交付形式偏差，而不冒险重写历史。

如果仓库管理规则硬性要求五个 PR，则不得在当前工作区直接拆分；必须从 `origin/main` 创建新 worktree，以 `backup/simplification-61fb86bc` 为源按以下批次重建，并在每个边界重新生成锁文件和运行对应测试：

1. UI 死代码与直接依赖删除。
2. Rust 标准库替换、掩膜共享和 AI trait 内联。
3. Rust 生成式测试客户端删除与本地 DTO 客户端。
4. UI MSW 随机模拟层删除与确定性 fixture。
5. UI fetch 客户端、稳定 facade、生成物门禁与本次修复。

通过标准：默认路径下保留可回滚的保护分支，只增加一个聚焦修复提交；若启用五 PR 硬性路径，每个 PR 都必须独立通过冻结锁文件安装和对应测试。

### [ ] 7. 推送 PR 并完成跨平台验证

1. 推送前再次确认只有预期提交和两个已知未跟踪用户路径。
2. 推送修复分支并创建 PR。PR 不得在 CI 前合并。
3. 等待并记录以下 GitHub Actions 结果：
   - Build：macOS latest、Windows 2022、Ubuntu latest、Ubuntu 24.04 ARM。
   - Test：Linux Rust workspace tests、UI tests。
   - Lint：Rust fmt/check/clippy、UI lint。
   - Test：generated artifact checker 回归测试和 generated artifacts drift check。
4. 任一平台失败时，只修复该平台根因，不顺带清理其他基线 warning。
5. Windows 重点检查 `File::try_lock()` 的编译和二次打开拒绝测试；Linux 重点检查 integration client 和 workspace tests；macOS 重点检查 Metal 构建。

通过标准：所有必需 CI job 成功，没有以 rerun 掩盖可重现失败。

### [ ] 8. 最终审查与收口

1. 运行 `codex-review` 正确性审查，重点检查生成物检测的退出码、路径边界和 Windows 兼容性。
2. 运行 `ponytail-review` 差异审查，确认没有为一个 Git 检查引入多层抽象或新依赖。
3. 检查最终差异只包含：
   - `scripts/check-generated.ts`
   - 对应回归测试
   - `package.json`
   - `.github/workflows/test.yml`
   - 必要的格式化或锁文件变化
4. 不手写 CHANGELOG，发布说明继续使用现有 git-cliff 流程。
5. 将验证命令、结果、CI 链接、已接受的传递依赖和基线失败写入 PR 说明。

最终停止条件：生成物漂移门禁已能发现未跟踪文件，本地检查全部通过或仅剩文档中明确的既有基线，跨平台 CI 全绿，用户未跟踪文件保持不变，没有未处理的高或中严重度审查问题。

## 接受的残留项

- `@faker-js/faker` 和 `@orval/mock` 仍可由 `orval` 元包传递引入；它们不再是项目直接依赖，也没有运行时或测试代码引用。为此替换 Orval CLI 会引入更多自定义生成基础设施，本次不做。
- `once_cell` 和 `num_cpus` 可继续作为其他第三方 crate 的传递依赖；本次只要求 Koharu 不再直接使用。
- `windows-sys` 在 `koharu-runtime` 中仍有实际 Windows loader 用途；只删除 `koharu` crate 的冗余声明，不能从 workspace 强制移除。
- UI 的 14 条已有 warning、macOS CUDA 测试基线和 all-targets Clippy `identity_op` 单独建立后续任务，不与本次门禁修复混合。
