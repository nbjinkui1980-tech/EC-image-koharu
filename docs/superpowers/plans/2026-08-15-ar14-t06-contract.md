# Lane 执行合同:L-AR14-T06 — CI 完整门禁(单卡)

- 状态:**Phase 3 一次批准已覆盖(2026-08-13 授予)**;LOOP-3 本地认领已登记
- 认领基线:Phase 3 起点 main `b68f123e`;执行分支 `audit-remediation-phase3`(认领时 tip `c375d92f`)
- 提交/回滚单元:单卡 commit;lane 收口 docs(evidence) 单独一个 commit
- 前置依赖:AR10-T03 ✅`4b8c5caa`(已在分支)
- **§0.2 执行约束**:仅本地修改 workflows,不 push、不触发远端;"实际 PR workflow"验证因远端暂停记录为环境约束,门禁以 policy test 为准
- 执行环境偏差(继承):oracle 可用;收口独立 review 沿用 oracle
- 串行点声明:CI lane——门禁为 policy test;不占 Cargo target

## 范围文件域(域外改动禁止)

| 卡 | 允许文件 |
|---|---|
| AR14-T06 | `scripts/supply-chain-policy.test.ts`、`.github/workflows/lint.yml`、`.github/workflows/test.yml`(≤3) |

## 卡:AR14-T06 — CI 完整门禁

- **验收标准(TASKS 原文)**:依赖:AR10-T03;独占 workflows。文件:`scripts/supply-chain-policy.test.ts`、`.github/workflows/lint.yml`、`test.yml`。RED:CI 缺 workspace/all-targets、fmt、Clippy、UI format/lint/test、generated、cargo/bun audit 任一阻断步骤。GREEN:全部阻断;allowlist 含 advisory/reachability/owner/expiry 且过期失败。验证:policy test、actionlint、实际 PR workflow。
- **现状(RED-0 实证)**:lint.yml 用裸 `cargo fmt -- --check`/`cargo check`/`cargo clippy -- -D warnings`(缺 `--all`、`--workspace --all-targets`);UI job 仅 lint(缺 format:check);两文件均无 `cargo audit`/`audit:deps`;test.yml 已有 workspace tests + check:generated + UI test ✓;CI 用裸 cargo(非 `bun cargo`——guard 强制 `/Volumes/G` target,CI 无此卷,裸 cargo 是 CI 正确形态,本地 §9 用 `bun cargo` 是共享 target 纪律,两域不混)。
- **设计**:
  - policy test 追加 CI 门禁断言(Rust 全套/UI 全套/generated/双 audit);allowlist 语义(advisory/reachability/owner/expiry+过期失败)已由既有 audit 测试覆盖,作锁不重复
  - lint.yml:Rust job 三命令补齐 `--all`/`--workspace --all-targets`;UI job 加 `bun run format:check`;新增 `audit` job(cargo install cargo-audit --locked → `cargo audit` + `bun run audit:deps`)
  - lint.yml/test.yml 既有 `uses:` 同步钉 SHA(T01 已解析值复用);pin 政策测试的文件域扩到全部五文件(T06 卡域内含 test.yml/lint.yml)
- **RED 断言**(policy test):
  1. `CI blocks on the full workspace rust gate set` — 缺 `--all`/`--workspace --all-targets` → FAIL
  2. `CI blocks on the UI format/lint/test gate set` — 缺 format:check → FAIL
  3. `CI blocks on generated-artifact drift` — 锁(现状已有)
  4. `CI blocks on cargo and dependency audit` — 无 audit 步骤 → FAIL
  5. lint/test 的 uses: 钉 SHA(政策域扩展后同 T01 双断言)
- **目标文件**:上表(≤3)
- **验收命令**:`bun test scripts/supply-chain-policy.test.ts`
- **证据**:
  - RED 3F/16P(缺 `--all`/`--workspace --all-targets`、缺 format:check、无 audit 步骤;generated 锁通过)→ GREEN 19/19;commit `f31158fc`
  - 决策点:CI 用裸 cargo(guard 强制 `/Volumes/G` target,CI runner 无此卷——裸 cargo 是 CI 正确形态;本地 §9 `bun cargo` 是共享 target 纪律);cargo-audit 经 `cargo install --locked` 就地安装
  - pin 政策文件域扩至全部五 shipping/CI workflows(lint/test 既有 uses: 用 T01 解析值钉入)

## Lane 收口(2026-08-15)

- 门禁:policy 19P/0F、workspace check 净、YAML 结构验证(lint rust/ui/audit 三 job);actionlint 工具缺失(记录);"实际 PR workflow"验证因远端暂停记环境约束(§0.2)
- 独立 review:oracle 第 1 次 30min 无状态超时、第 2 次账户余额不足(Payment Required)→ 按先例降级对抗性自审:零 blocker/major;2 informational(audit job 未配 bun 缓存——仅性能;子串式政策断言对未来维护者可绕——既有模式,记录);审计 job 无 continue-on-error,失败即阻断
- 提交/回滚单元:T06 `f31158fc`,可独立 revert
- 依赖传播:无下游(FINAL-T01 等全卡 ✅ + 全 wave PASS);W6 差 AR14-T03A~E ◐ 证据补录
